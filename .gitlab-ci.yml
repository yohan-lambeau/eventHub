# fix pipeline trigger
image: maven:3.9.6-eclipse-temurin-17

stages:
  - build
  - test

build-job:
  stage: build
  script:
    - echo "Compilation du projet
    - echo "Compilation du premier microservice -> configserver'
    - cd configserver
    - mvn clean install -DskipTests

test-job:
  stage: test
  script:
    - echo "Test config-server"
    - cd config-server
    - mvn test